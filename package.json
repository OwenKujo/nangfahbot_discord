{
  "name": "nangfahbot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "node deploy-commands.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "curl -L https://github.com/yt-dlp/FFmpeg-Builds/releases/latest/download/ffmpeg-master-latest-win64-gpl.zip -o ffmpeg.zip && tar -xf ffmpeg.zip && mkdir -p ./ffmpeg && mv ffmpeg-*/* ./ffmpeg/ && export PATH=$PATH:$(pwd)/ffmpeg/bin"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "@discordjs/voice": "^0.18.0",
    "@distube/spotify": "^2.0.2",
    "discord.js": "^14.20.0",
    "distube": "^5.0.7",
    "dotenv": "^16.5.0",
    "ytdl-core": "^4.11.5"
  }
}
